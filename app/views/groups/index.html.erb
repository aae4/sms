<h1>Группы контаков</h1>
<ul>
<% @groups.each do |group| %>
	<li><%= link_to group.name, group%></li>
<%end%>
</ul>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_group_path,
            :class => 'btn btn-primary' %>
